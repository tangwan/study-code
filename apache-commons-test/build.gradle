project(':apache-commons-test') {
    bootRepackage.enabled = false
    jar.enabled = false
    bootRepackage.withJarTask = jar

    dependencies {
        compile group: 'org.projectlombok', name: 'lombok', version: '1.16.10'
    }
}